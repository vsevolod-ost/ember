# Словник термінів

Курс: [EN](README.md) · [UK](README.uk.md) · Довідник машини: [ISA.uk.md](ISA.uk.md)

Українською — те, як ми говоримо. **English** — те, що вводити в пошук: cppreference,
learncpp, Stack Overflow і повідомлення компілятора живуть англійською, і це не
зміниться. Обидві колонки однаково потрібні.

**Що ми не перекладаємо ніколи.** Мнемоніки (`LOADI`, `JZ`, `PUSH`), імена регістрів
(`A`, `B`, `H`, `PC`, `SP`), прапорці (`Z`, `N`, `C`), команди `ember` (`dump`, `get`,
`regs`, `step`, `run`), прапорці компілятора й дослівні повідомлення на кшталт
`detect_leaks is not supported on this platform`. Це мова машини, а не текст.

---

## Як програма стає програмою

| Українською | English | Що це | Лаба |
|---|---|---|---|
| компілятор | compiler | перекладає ваш текст у машинний код | 1 |
| лінкер | linker | зшиває кілька `.o` в одну програму; свої помилки має окремі | 1 |
| одиниця трансляції | translation unit | один `.cpp` разом з усім, що він включив | 7 |
| заголовок | header file | `.hpp`: оголошення без тіл, обіцянка для інших файлів | 7 |
| збірка | build | те, що робить `cmake --build` | 1 |
| санітайзер | sanitizer | перевірки, вшиті в бінарник на етапі компіляції | 1 |
| невизначена поведінка | undefined behaviour, UB | мова не обіцяє, що станеться | 1 |
| попередження як помилка | warnings as errors, `-Werror` | компілятор не пропускає підозріле | 1 |

## Числа й біти

| Українською | English | Що це | Лаба |
|---|---|---|---|
| байт | byte | 8 бітів; одна клітинка пам'яті `ember` | 1 |
| літерал | literal | значення, записане прямо в коді: `65`, `0x41`, `'A'` | 1 |
| беззнаковий / знаковий | unsigned / signed | чи вважаємо старший біт знаком | 1 |
| переповнення | overflow | результат не вліз у тип | 1 |
| загортання | wraparound, wrap | після 255 знову 0; для беззнакових це визначено | 1 |
| число з рухомою комою | floating point | `float`, `double`; більшість десяткових дробів у них неточні | 1 |
| доповняльний код | two's complement | як тими самими бітами записують від'ємні числа | 2 |
| маска | mask | значення, у якому одиниці стоять лише на потрібних бітах | 2 |
| зсув | shift | `<<` і `>>` | 2 |
| прапорець | flag | один біт стану: `Z`, `N`, `C` | 2 |
| перенесення | carry | біт, який виїхав за межу восьми | 2 |
| позика | borrow | те саме при відніманні | 2 |
| пріоритет операцій | operator precedence | чому `x & 1 == 0` означає не те, що здається | 2 |

## Машина

| Українською | English | Що це | Лаба |
|---|---|---|---|
| віртуальна машина | virtual machine, VM | програма, яка поводиться як процесор і пам'ять | 1 |
| дамп | dump, hexdump | надрукувати пам'ять шістнадцятково й ASCII | 1 |
| регістр | register | іменована комірка всередині процесора | 2 |
| опкод | opcode | байт, який каже, яка це інструкція | 2 |
| ALU | arithmetic logic unit | блок, який додає, множить, робить AND, зсуває | 2 |
| набір інструкцій | instruction set, ISA | таблиця, яка є контрактом машини | 2 |
| регіон | region | іменована ділянка карти пам'яті: CODE, DATA, VRAM, HEAP, STACK | 2 |
| ділянка | span, range | довільний шматок пам'яті, як-от `[lo, hi)` | 4 |
| гість і хост | guest and host | числа всередині `ember` проти вашого процесу C++ | 3 |

## Пам'ять і вказівники

| Українською | English | Що це | Лаба |
|---|---|---|---|
| вказівник | pointer | змінна, значення якої є адресою | 3 |
| розіменування | dereference | піти за адресою: `*p` | 3 |
| нульовий вказівник | null pointer, `nullptr` | вказівник, який нікуди не вказує | 3 |
| арифметика вказівників | pointer arithmetic | `p + 1` рухає на **елемент**, а не на байт | 3 |
| порядок байтів, молодший уперед | endianness, little-endian | `0x1234` лягає як `34 12` | 3 |
| безпосередній операнд | immediate | значення прямо в інструкції, а не за адресою | 3 |
| адресний регістр | address register, index register | `H`: тримає «де», а не «що» | 3 |
| вихід за межі | out-of-bounds, buffer overflow | `a[4]` у масиві на чотири елементи | 3 |
| час життя | lifetime, storage duration | коли пам'ять виникає і коли зникає | 6 |
| стек | stack | автоматична пам'ять; тут же кадри викликів | 6 |
| купа | heap | пам'ять, час життя якої обираєте ви | 6 |
| bump-алокатор | bump allocator | «відріж n байтів, посунь вершину»; `free` немає | 6 |
| витік пам'яті | memory leak | об'єкт живий, вказівника на нього вже немає | 6 |
| завислий вказівник | dangling pointer | вказівник живий, об'єкта вже немає | 6 |
| подвійне звільнення | double free | два `delete` на один `new` | 6 |

## Керування

| Українською | English | Що це | Лаба |
|---|---|---|---|
| коротке замикання | short-circuit evaluation | `&&` не рахує праве, якщо ліве вже хибне | 4 |
| провалювання | fall-through | `case` без `break` іде далі вниз | 4 |
| затінення | shadowing | внутрішнє ім'я ховає зовнішнє | 4 |
| область видимості | scope | де ім'я взагалі видно | 4 |
| стрибок | jump, branch | записати нову адресу в `PC` | 4 |
| умовний стрибок | conditional branch | стрибнути, лише якщо прапорець виставлений | 4 |
| лінійний пошук | linear search | пройти підряд, поки не збіглось | 4 |

## Дані

| Українською | English | Що це | Лаба |
|---|---|---|---|
| масив | array | багато значень одного типу поспіль | 5 |
| зведення до вказівника | array decay | ім'я масиву у виразі стає вказівником, розмір губиться | 5 |
| крок рядка | stride | скільки байтів від початку рядка до початку наступного | 5 |
| C-рядок | C string | байти до нульового термінатора | 5 |
| кадровий буфер, відеопам'ять | framebuffer, VRAM | 256 байтів на `0xA00`, по біту на піксель | 5 |
| сортування бульбашкою | bubble sort | сусідні обміни, `O(n²)` | 5 |
| сортування вставками | insertion sort | посунути наступний елемент у відсортований початок | 5 |
| структура | struct | набір іменованих полів поруч у пам'яті | 6 |
| перелік | enumeration, `enum class` | іменований набір невеликих цілих | 6 |
| вирівнювальні байти | padding | дірки в структурі заради вирівнювання полів | 6 |
| посилання | reference, `T&` | вказівник, який не буває порожнім і не потребує `*` | 6 |
| абстрактний тип даних | abstract data type, ADT | інтерфейс назовні, представлення сховане | 7 |
| зв'язний список | linked list | вузли, з'єднані вказівниками; довжина наперед невідома | 8 |
| черга | queue | FIFO: перший прийшов, перший пішов | 7 |

## Виклики

| Українською | English | Що це | Лаба |
|---|---|---|---|
| кадр стека | stack frame | локальні дані й адреса повернення одного виклику | 7 |
| адреса повернення | return address | куди `RET` поверне керування | 7 |
| конвенція виклику | calling convention | де аргумент, де результат, хто рятує регістри | 7 |
| вказівник стека | stack pointer, `SP` | де зараз вершина стека | 7 |
| порожній спадний стек | empty descending stack | `SP` вказує на вільний байт і йде вниз | 7 |
| переповнення стека | stack overflow | стек уперся в межу регіону | 7 |
| спустошення стека | stack underflow | зняли більше, ніж клали | 7 |
| рекурсія | recursion | функція викликає сама себе | 7 |
| базовий випадок | base case | умова, за якої рекурсія зупиняється | 7 |
| хвостова рекурсія | tail recursion | виклик є останньою дією; компілятор *може* згорнути в цикл | 7 |
| передача за значенням | pass by value | функція отримує копію | 7 |
| передача за посиланням | pass by reference | функція отримує сам об'єкт | 7 |

## Мова

| Українською | English | Що це | Лаба |
|---|---|---|---|
| лексема | lexeme | шматок тексту: `LOADI`, `65`, `,` | 8 |
| токен | token | лексема плюс її вид і номер рядка | 8 |
| лексер, сканер | lexer, scanner | перетворює символи на токени або на помилку | 8 |
| асемблер | assembler | перетворює токени на байти | 8 |
| дизасемблер | disassembler | назад: з байтів у читабельний лістинг | 8 |
| мітка | label | ім'я для адреси: `loop:` | 8 |
| два проходи | two-pass assembly | спершу зібрати мітки, потім видати байти | 8 |
| синтаксична помилка | syntax error | рядок не належить мові; повідомити з номером рядка | 8 |
| граматика | grammar | правила, за якими рядок є або не є програмою | 8 |
| дерево розбору | abstract syntax tree, AST | структура виразу; у нас лише в Stretch | 8 |

---

## Кілька слів, які легко сплутати

- **Регіон** — це іменована ділянка карти пам'яті (`DATA`, `VRAM`). **Ділянка** — будь-який шматок, наприклад «вісім байтів на `0x800`».
- **Витік** і **завислий вказівник** — протилежності. У першому зник вказівник, у другому зник об'єкт.
- **Перенесення** (carry) — при додаванні. **Позика** (borrow) — при відніманні. Прапорець той самий, `C`.
- **Переповнення** (overflow) — про число, яке не влізло в тип. **Переповнення стека** (stack overflow) — про пам'ять. Слово одне, речі різні.
- **Лексема** — це текст. **Токен** — це лексема, у якої вже є вид і номер рядка.
